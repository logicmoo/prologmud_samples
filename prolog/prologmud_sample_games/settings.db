/*  Saved settings
    Date: Mon Jun 19 21:09:46 2017
*/


%	Name the outside world can use to contact me
setting(http:public_host, 'prologmoo.com').

%	Port on the public server
setting(http:public_port, 3050).

%	IP addresses from which remotes are allowed to connect
setting(pengines:allow_from, [*]).

%	Backend to use (name of the module
setting(user_profile:backend, impl_profile_prolog).

%	Version of jquery served by the html resource "jquery"
setting(jquery:version, '2.1.3.min').

%	Name of the SMTP host for relaying the mail
setting(smtp:host, '').

%	Port on which the SMTP host listens (0: default)
setting(smtp:port, 0).

%	Security system to use
setting(smtp:security, none).

%	Default password for smtp:user
setting(smtp:password, ****).

%	Keep information to support source-level debugging
setting(swish:debug_info, true).

%	Url of the server itself
setting(http:server_url, 'http://prologmoo.com:3020').
